# Kamiwaza Flow App Garden Package

This package wraps the repository's Kamiwaza Flow frontend, gateway, and LangGraph runtime as a Kamiwaza App Garden app.

The source images are built from the repository root by running `make build-no-cache` at the repo root. The generated template is pushed to a local Kamiwaza instance with `make kamiwaza-push`.
